fastapi>=0.110.0,<0.112.0
uvicorn[standard]>=0.28.0,<0.30.0
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
sqlalchemy[asyncio]>=2.0.28,<2.1.0
asyncpg>=0.29.0,<0.30.0
aiosqlite>=0.20.0
alembic>=1.13.1,<1.14.0
python-dotenv>=1.0.1,<1.1.0
psycopg2-binary>=2.9.9
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.2,<4.2.0
pyjwt>=2.8.0,<3.0.0
cryptography>=42.0.0
# Google Sign-In — server-side ID token verification (signature, issuer,
# audience, expiry). See app/services/google_identity_service.py. `requests`
# is google-auth's HTTP transport for fetching Google's signing certs.
google-auth>=2.28.0,<3.0.0
requests>=2.31.0,<3.0.0
openpyxl>=3.1.0,<3.2.0
Pillow>=10.3.0,<11.0.0
# opencv-python-headless 4.9.x is built against the NumPy 1.x ABI — pinning
# numpy<2 explicitly avoids the "_ARRAY_API not found" import crash that
# happens when pip resolves numpy 2.x as an "any version" transitive dep.
numpy>=1.26.0,<2.0.0
opencv-python-headless>=4.9.0,<4.10.0
# Phase B — Design Studio rendering engine: local QR generation and
# multi-product catalogue PDF assembly. Both pure-Python, no system deps.
qrcode>=7.4.0,<8.0.0
reportlab>=4.1.0,<4.3.0
# Module 31 — in-process scheduler for the market rate sync job (see
# app/scheduler/market_rate_scheduler.py). No broker/worker infra required —
# runs inside the existing single uvicorn process.
apscheduler>=3.10.4,<3.11.0

# Testing dependencies
pytest>=7.4.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
anyio>=4.0.0
